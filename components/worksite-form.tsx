"use client";
import { useState } from "react";
export function WorksiteForm({ editing = false }: { editing?: boolean }) { const [saved, setSaved] = useState(false); return <form className="admin-card worksite-form" onSubmit={(event) => { event.preventDefault(); setSaved(true); }}><div className="form-section"><h2>Worksite details</h2><label>Name<input defaultValue={editing ? "Harbor Street Kitchen" : ""} required/></label><label>Address line 1<input defaultValue={editing ? "412 Harbor Street" : ""} required/></label><div className="three-columns"><label>City<input defaultValue={editing ? "Oakland" : ""} required/></label><label>State<input defaultValue={editing ? "CA" : ""} required/></label><label>Postal code<input defaultValue={editing ? "94607" : ""} required/></label></div><label>Timezone<select defaultValue="America/Los_Angeles"><option>America/Los_Angeles</option><option>America/Denver</option><option>America/Chicago</option><option>America/New_York</option></select></label></div><div className="form-section"><h2>Location verification</h2><p className="muted">Location is captured only at configured clock events. It is never continuously tracked.</p><label className="check-label"><input type="checkbox" defaultChecked/>Require a location result</label><label>Behavior<select defaultValue="FLAG"><option value="FLAG">FLAG — allow and mark for review</option><option value="STRICT">STRICT — block when unverified</option><option value="OPTIONAL">OPTIONAL — location not required</option></select></label><label>Geofence radius (meters)<input type="number" min="25" max="5000" defaultValue="150"/></label><div className="two-columns"><label>Latitude<input type="number" step="any" defaultValue={editing ? "37.7955" : ""}/></label><label>Longitude<input type="number" step="any" defaultValue={editing ? "-122.2787" : ""}/></label></div></div><button className="button primary">{editing ? "Save changes" : "Create worksite"}</button>{saved && <p className="alert success">Worksite settings saved in this interface preview.</p>}</form>; }
