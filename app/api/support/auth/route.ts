import {NextRequest,NextResponse} from "next/server";import{z}from"zod";import{getCookieSupabase}from"@/lib/supabase/server";import{getAdminSupabase}from"@/lib/supabase/admin";
const schema=z.object({email:z.string().email(),password:z.string().min(12).max(200)});const denied=()=>NextResponse.json({error:"The sign-in details could not be verified."},{status:401});
export async function POST(request:NextRequest){try{const input=schema.parse(await request.json());const client=await getCookieSupabase();const{data,error}=await client.auth.signInWithPassword(input);if(error||!data.user)return denied();const platform=await getAdminSupabase().from("platform_users").select("id").eq("user_id",data.user.id).eq("status","ACTIVE").limit(1).maybeSingle();if(!platform.data){await client.auth.signOut();return denied();}return NextResponse.json({ok:true});}catch{return denied();}}
